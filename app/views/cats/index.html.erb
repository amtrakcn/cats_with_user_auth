<h2>Cats</h2>
<!-- welcome bob -->
<% if !current_user.nil? %>
<h3>Welcome <%= current_user.user_name %>!</h3>
<% end %>

<ul>
  <% @cats.each do |cat| %>
	  <li><%= link_to cat.name, "#{cats_url}/#{cat.id}" %></li>
  <% end %>
</ul>